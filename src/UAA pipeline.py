import pandas as pd 
rom collections import defaultdict
from collections import Counter

class UAAPipeline(self):
    def __init__(self,filename='results'):
        self.filename=filename
        self.temp_file=temp_file
        self.old_features=old_features
        self.clean_dict=defaultdict{lambda:'None',
                                    'drought_data':{'filename':'data/Drought Data.csv',
                                                    'clean':      }
     
                                    }



    def load(self, parameter_list):
        self.temp_file=pd.read_csv(f'data/{self.filename}.csv')

    def rename(self):
        self.old_features=self.temp_file.columns
        if self.filename in self.clean_list:
            
    def drop_columns(self, parameter_list):
        pass

    def if __name__ == "__main__":
        load(filename)
        rename()
        clean()
        create_columns()
        return output_file
        pass